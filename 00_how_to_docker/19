mkdir $HOME/root
cp hello.py $HOME/root
docker pull python:2-slim
docker run --detach --tty --name Abathur --volume $HOME/root:/root --workdir /root --publish 3000:3000 --env FLASK_APP=hello.py python:2-slim
docker exec Abathur Flask
docker exec Abathur flask run --host=0.0.0.0 --port=3000
